import { Routes } from '@angular/router';
import { HubComponent } from './presentation/components/hub/hub.component';

export const routes: Routes = [
  {
    path: 'hub',
    component: HubComponent,
  },
];
